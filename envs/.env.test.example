# =============================================================================
# TESTING ENVIRONMENT CONFIGURATION
# =============================================================================
# Example testing environment configuration
# Copy this file to envs/.env.test and adjust values
# =============================================================================

# Project configuration
COMPOSE_PROJECT_NAME=myapp-test
COMPOSE_PROFILES=test

# Database configuration
DB_NAME_TEST=appdb_test
DB_USER_TEST=appuser_test

DB_NAME=appdb_dev
DB_USER=appuser_dev

# Resource limits
APP_CPU_LIMIT=0.3
APP_MEMORY_LIMIT=384M
DB_CPU_LIMIT=0.2
DB_MEMORY_LIMIT=192M

# Use tmpfs for faster database operations (data lost on container stop)
# This is ideal for testing where persistence is not required

# Notes:
# 1. Use different database names for different test types
# 2. Consider using test-specific secrets
# 3. Database is ephemeral (data lost on container stop)
# 4. Use test-unit profile for unit tests without database
# 5. Use test-e2e profile for full end-to-end testing
