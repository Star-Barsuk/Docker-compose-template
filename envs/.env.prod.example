# =============================================================================
# PRODUCTION ENVIRONMENT CONFIGURATION
# =============================================================================
# Example production environment configuration
# Copy this file to envs/.env.prod and adjust values
# =============================================================================

# Project configuration
COMPOSE_PROJECT_NAME=myapp-prod
COMPOSE_PROFILES=prod-standard

# Application settings
APP_PORT_PROD=8001

# Database configuration
DB_NAME_PROD=appdb_prod
DB_USER_PROD=appuser_prod

DB_NAME=appdb_dev
DB_USER=appuser_dev

# PgAdmin configuration
PGADMIN_EMAIL=admin@yourdomain.com
PGADMIN_PORT_PROD=8081

# Image versions
PGADMIN_IMAGE=dpage/pgadmin4:8.12
POSTGRES_IMAGE=postgres:18.1-bookworm

# Resource limits
APP_CPU_LIMIT=0.5
APP_MEMORY_LIMIT=512M
DB_CPU_LIMIT=1.0
DB_MEMORY_LIMIT=2G
PGADMIN_CPU_LIMIT=0.2
PGADMIN_MEMORY_LIMIT=256M

# Healthcheck limits
HEALTHCHECK_INTERVAL=30s
HEALTHCHECK_START_PERIOD=60s

# Notes:
# 1. Create secrets: openssl rand -base64 32 > docker/secrets/db_password.txt
# 2. Adjust resource limits based on production server capacity
