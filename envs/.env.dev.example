# =============================================================================
# DEVELOPMENT ENVIRONMENT CONFIGURATION
# =============================================================================
# Example development environment configuration
# Copy this file to envs/.env.dev and adjust values
# =============================================================================

# Project configuration
COMPOSE_PROJECT_NAME=myapp-dev
COMPOSE_PROFILES=dev-standard

# Application settings
APP_PORT_DEV=8000

# Database configuration
DB_NAME_DEV=appdb_dev
DB_USER_DEV=appuser_dev
DB_PORT_DEV=5432

# PgAdmin configuration
PGADMIN_EMAIL=admin@yourdomain.com
PGADMIN_PORT_DEV=8080

# Image versions
PGADMIN_IMAGE=dpage/pgadmin4:8.12
POSTGRES_IMAGE=postgres:18.1-bookworm

# Resource limits
APP_CPU_LIMIT=0.2
APP_MEMORY_LIMIT=256M
DB_CPU_LIMIT=0.3
DB_MEMORY_LIMIT=256M
PGADMIN_CPU_LIMIT=0.1
PGADMIN_MEMORY_LIMIT=128M

# Healthcheck limits
HEALTHCHECK_INTERVAL=60s
HEALTHCHECK_START_PERIOD=90s

# Notes:
# 1. Create secrets: openssl rand -base64 32 > docker/secrets/db_password.txt
# 2. Adjust resource limits based on your system
